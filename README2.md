//Psuedocode & Explainations

createEmployeeRecord
    //Psuedocode
    Arguments: A 4-element array of a: string, string, string, & number corresponding to a first name, family name, title, & pay rate per hour
    Returns: JS object with keys: firstName, famName, title, payPerHour, timeInEvents, timeOutEvents
    Behaviors: Loads array elements into corresponding object properties. Additionally, initialize empty arrays on the properties timeInEvents & timeOutEvents